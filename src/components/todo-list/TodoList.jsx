import React from "react";

import { saveData, isDataSaved, removeData } from "../../utils";

const TodoList = () => {
  return (
    <div>
      <h1>Todo List</h1>
    </div>
  );
};

export default TodoList;